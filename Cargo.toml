[package]
name = "rmap"
version = "0.1.0"
description = "Nmap-like tool written in Rust"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }
